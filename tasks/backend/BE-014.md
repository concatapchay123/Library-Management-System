# BE-014 — Copy status transition and append-only history

## Task information

- Owner: Backend
- Status: Completed
- Priority: P1

## Outcome

Enforce physical-copy status transitions and retain an immutable actor-attributed history.

## Dependencies

- BE-013
- BE-005

## Interfaces and source of truth

- Consumes: copy persistence from BE-013 and audit transaction contract from BE-005.
- Produces: core.copy_status_history and status-transition application service.

## In scope

- Define allowed transitions among available, borrowed, lost, damaged and maintenance.
- Persist append-only history with actor, timestamp and reason.
- Make status change, history and audit record atomic.

## Out of scope

- Checkout locking, loan lifecycle, reservations and copy repair workflow.

## Test-first sequence

1. Add backend/tests/unit/core/test_copy_status.py for allowed and rejected transitions.
2. Run: cd backend; python -m pytest tests/unit/core/test_copy_status.py -q. Record the missing transition-policy failure.
3. Implement the domain policy, history migration and transactional service.
4. Run the unit test and backend/tests/integration/ops/test_audit_outbox.py.

## Evidence checkpoint

- Invalid transitions return the documented domain problem type.
- Every accepted transition has exactly one history record and one audit record.

## Evidence

- RED: `cd backend; python -m pytest tests/unit/core/test_copy_status.py -q` failed
  during collection with `ModuleNotFoundError: No module named
  'openlibrary.modules.core.application.copy_status'` before copy status code existed.
- GREEN: `cd backend; python -m pytest tests/unit/core/test_copy_status.py
  tests/unit/migrations/test_copy_status_history_migration.py
  tests/contract/test_inventory_openapi.py
  tests/integration/ops/test_audit_outbox.py -q` produced `45 passed, 4 skipped in 0.43s`.
  It covers allowed and rejected copy status transitions, state machine invariants,
  domain problem type URI (`https://openlibraryos.example/problems/invalid-copy-status-transition`),
  atomic mutation and audit writing via `AuditedTransaction`, immutable append-only history,
  mandatory actor and reason validation, and tenant isolation.
- API integration: `cd backend; python -m pytest tests/api/core/test_inventory.py -q`
  produced `7 passed in 0.39s`, validating both direct (`/api/v1/copies/{copy_id}/status`,
  `/api/v1/copies/{copy_id}/history`) and nested (`/api/v1/books/{book_id}/copies/{copy_id}/status`,
  `/api/v1/books/{book_id}/copies/{copy_id}/history`) HTTP endpoints with RFC Problem Details.
- Full suite: `cd backend; python -m pytest -q` produced `139 passed, 19 skipped in 3.97s`.
- Static analysis & quality gates: `python -m ruff check .` passed cleanly; `python -m ruff format --check .`
  verified 94 files already formatted; `python -m mypy src` reported `Success: no issues found in 47 source files`;
  `git diff --check` passed with 0 errors.
- OpenAPI contract: `python -m openapi_spec_validator ../contracts/openapi/v1.yaml` returned `OK`;
  `python ../contracts/openapi/check_compatibility.py ../contracts/openapi/v1.yaml ../contracts/openapi/v1.yaml`
  passed with no breaking changes.
- Review: `ocr delegate preview` and `ocr delegate rule` reviewed all 8 reviewable files against Python
  and YAML rulesets with 0 critical and 0 high findings.

## Acceptance criteria

- Lost, damaged and maintenance copies cannot enter a borrowable state without an explicit allowed transition.
- History rows cannot be updated or deleted through the application service.
- Status logic is copy-level, never book-level.

## Reviewer checklist

- Status and audit writes share one transaction.
- Transition reason and actor are required for manual inventory changes.
