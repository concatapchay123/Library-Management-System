# BE-011 — Tenant middleware, pool cleanup, organization settings and RLS catalog enforcement

## Task information

- Owner: Backend
- Status: Completed
- Priority: P0

## Outcome

Set authenticated tenant context safely for every request and prove tenant protections remain complete as tables are added.

## Dependencies

- BE-004
- BE-010

## Interfaces and source of truth

- Consumes: verified principal from BE-008, shared predicate from BE-004 and tenant rules in docs/database-design.md.
- Produces: transaction-scoped SESSION_CONTEXT middleware, organization settings API, bootstrap command and catalog verifier.

## In scope

- Set organization context before the first protected query and clear it before connection-pool release.
- Invalidate a connection when cleanup fails.
- Add privileged organization bootstrap and authorized organization-settings operations.
- Validate that every tenant table has organization_id, composite tenant relations and filter/block RLS predicates.

## Out of scope

- Public organization administration, cross-tenant user APIs and new business-domain tables.

## Test-first sequence

1. Add backend/tests/integration/tenancy/test_tenant_context.py for cross-tenant read/write denial, pool reuse and policy-free fixture detection.
2. Run: cd backend; python -m pytest tests/integration/tenancy/test_tenant_context.py -q. Record the missing middleware/catalog failure.
3. Implement context lifecycle, catalog verifier, settings use case and bootstrap command.
4. Run the focused integration suite against SQL Server.

## Evidence checkpoint

- Tenant A followed by Tenant B on a pooled connection cannot retain A context.
- A deliberately policy-free tenant fixture fails the catalog check.

## Evidence

- RED: `cd backend; python -m pytest tests/integration/tenancy/test_tenant_context.py -q`
  failed during collection with `ModuleNotFoundError: No module named
  'openlibrary.modules.core.infrastructure.tenancy'` before implementation.
- GREEN (focused real SQL Server): with a temporary SQL-auth bootstrap identity,
  distinct temporary migration/runtime identities, and a disposable database on the
  local SQL Server instance, `cd backend; python -m pytest
  tests/integration/tenancy/test_tenant_context.py -q` produced `2 passed in
  5.32s`. It proves Tenant A cannot read/update/delete Tenant B, a foreign insert
  is rejected, a one-connection pool has no residual context after release, and a
  policy-free tenant table fails catalog verification. All temporary identities and
  the database were removed after the run.
- Final real SQL Server suite: under the same disposable three-identity setup,
  `cd backend; python -m pytest -q` produced `96 passed in 35.37s` with no skips.
  The temporary bootstrap login was removed after completion.
- Quality and contract checks: `python -m ruff format --check .`, `python -m ruff
  check .`, `python -m mypy src`, `python -m openapi_spec_validator
  ../contracts/openapi/v1.yaml`, and the V1 compatibility check against `HEAD`
  passed. `docker compose --env-file .github/ci/compose.env -f
  infra/docker-compose.yml config --quiet` and `git diff --check` passed.
- Review: delegated Open Code Review preview covered all 16 reviewable source and
  contract files; its four default-path-excluded tests plus the lifecycle unit test
  were manually reviewed. The review found and remediated a missing Problem Details
  `request_id` and strengthened pooled-connection proof to use `pool_size=1`.

## Acceptance criteria

- Missing tenant context fails closed.
- SQL Server blocks cross-tenant SELECT, INSERT, UPDATE and DELETE.
- Bootstrap is not reachable through an ordinary tenant-user API.

## Reviewer checklist

- Cleanup failure invalidates the physical connection.
- Catalog rules verify both RLS predicates and composite tenant foreign keys.
