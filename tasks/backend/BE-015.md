# BE-015 — Durable dispatcher, job records, lease, retry and deduplication

## Task information

- Owner: Backend
- Status: Completed
- Priority: P1

## Outcome

Deliver the generic worker protocol that safely claims durable outbox events and records bounded, replay-safe processing.

## Dependencies

- BE-005

## Interfaces and source of truth

- Consumes: ops.outbox_events from BE-005 and worker rules in docs/system-design.md.
- Produces: ops.job_records, ops.claim_outbox_event, dispatcher service, lease protocol and consumer-deduplication port.

## In scope

- Migrate job records and claim metadata.
- Atomically claim one event with a lease and server-derived organization context.
- Implement bounded retry, dead-letter state, lease reclamation and idempotent consumer recording.

## Out of scope

- Domain-specific reservation logic, email provider integration and notification persistence.

## Test-first sequence

1. Add backend/tests/integration/ops/test_dispatcher.py for single claim, crash/reclaim, dead-letter and duplicate delivery.
2. Run: cd backend; python -m pytest tests/integration/ops/test_dispatcher.py -q. Record the absent-dispatcher failure.
3. Implement migrations, claim procedure, dispatcher and consumer-deduplication port.
4. Run the focused integration test against SQL Server and Redis.

## Evidence checkpoint

- A worker crash after claim does not lose an event.
- Replayed delivery does not execute a consumer side effect twice.

## Acceptance criteria

- Consumer code receives tenant context only from the server-created event record.
- Retries are bounded and exhausted events are visible in a dead-letter state.
- Event payload version is checked before consumer handling.

## Reviewer checklist

- [x] No worker accepts a client-selected tenant id.
- [x] Lease expiry cannot silently skip an event.

## Evidence checkpoint verification

### TDD RED evidence — 2026-09-23

- Command: `cd backend; python -m pytest tests/integration/ops/test_dispatcher.py -q`
- Exit code: `1`
- Result: `7 failed` with `ImportError: cannot import name 'OutboxDispatcherService' from 'openlibrary.modules.ops.application.dispatcher'` and absent claim procedure before implementation existed.

### TDD GREEN evidence — 2026-09-23

- Unit tests command: `cd backend; python -m pytest tests/unit/ops/ tests/unit/migrations/test_outbox_claim_and_job_records_migration.py -v`
- Exit code: `0`
- Result: `6 passed in 0.42s`.
- Integration tests command: `cd backend; python -m pytest tests/integration/ops/test_dispatcher.py -v` (with SQL Server environment variables configured)
- Exit code: `0`
- Result: `7 passed in 5.73s`:
  - `test_dispatcher_classes_exist`: PASSED
  - `test_dispatcher_single_claim_and_delivery`: PASSED (atomic claim via `ops.claim_outbox_event`, tenant context switch, delivery confirmation)
  - `test_worker_crash_after_claim_does_not_lose_event`: PASSED (lease expiration allows reclamation by next worker without data loss)
  - `test_bounded_retry_and_dead_letter_visibility`: PASSED (retry count tracked, exponential backoff, dead-letter state upon retry exhaustion)
  - `test_replayed_delivery_does_not_execute_consumer_side_effect_twice`: PASSED (`ConsumerDeduplicationPort` idempotency guard prevents duplicate consumer action)
  - `test_unsupported_payload_version_dead_letters`: PASSED (unknown or unsupported payload versions dead-lettered immediately)
  - `test_tenant_isolation_in_consumer`: PASSED (RLS boundary enforced via `SESSION_CONTEXT(N'organization_id')`)

### SQL Server Reversibility & Tenant Catalog Verification — 2026-09-23

- Disposable database migrated through revision `0011_outbox_claim_jobs` then downgraded to `0010_copy_status_history`:
  - Downgrade cleanly removed RLS security predicates, dropped `ops.job_records`, dropped `ops.claim_outbox_event`, dropped `IX_ops_outbox_events_claim`, dropped `UQ_ops_outbox_events_tenant_event`, and dropped all added claim columns.
  - Verified `residual_claim_columns=0`, `residual_job_records_table=0`, `residual_claim_procedure=0`.
- Tenant catalog verification:
  - `verify_tenant_catalog` passed with `0 errors`.
  - `ops.job_records` adheres to RLS requirements (1 filter predicate, 3 block predicates), composite foreign keys `(organization_id, outbox_event_id)` referencing `ops.outbox_events(organization_id, event_id)`, and composite candidate keys.

### Static Analysis, Linting & Full Suite Verification — 2026-09-23

- Ruff check: `cd backend; python -m ruff check .` -> `All checks passed!`
- Ruff format: `cd backend; python -m ruff format --check .` -> `100 files already formatted`
- Mypy type-checking: `cd backend; python -m mypy src` -> `Success: no issues found in 49 source files`
- Full test suite: `cd backend; python -m pytest tests/ -q` (with SQL Server test configuration) -> `171 passed in 40.60s` (0 failed, 0 skipped)
- OpenAPI contract: `python -m openapi_spec_validator ../contracts/openapi/v1.yaml` -> `OK`

### Open Code Review (OCR) — 2026-09-23

- `ocr delegate preview`: 5 reviewable files inspected against system Python ruleset.
- Zero high or critical severity defects found.
- Parameterized queries with SQL text and parameter dicts throughout.
- Sensitive credentials/tokens scrubbed using regex redaction in `sanitize_error_message`.
- Warning logging applied to edge-case exception handling with structured format strings.
- Engine lifecycle managed cleanly with `dispose()`.
