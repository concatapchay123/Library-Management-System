# BE-016 — Loan request, approval, checkout and return lifecycle

## Task information

- Owner: Backend
- Status: Completed
- Priority: P1

## Outcome

Implement the core loan lifecycle with a single application service for self-service and librarian-desk flows.

## Dependencies

- BE-014
- BE-015
- BE-010

## Interfaces and source of truth

- Consumes: copy-state policy from BE-014, authorization from BE-010 and audit/outbox protocol from BE-005.
- Produces: core.loans, loan lifecycle service and documented loan endpoints.

## In scope

- Migrate loans with active-loan invariants and tenant constraints.
- Implement request, approval, direct desk checkout and return.
- Write audit and outbox records with each successful lifecycle mutation.

## Out of scope

- Concurrent checkout locking, idempotency key replay, reservation allocation, overdue jobs and edition policies.

## Test-first sequence

1. Add backend/tests/api/core/test_loans.py for request, approval, desk checkout, return and invalid state transitions.
2. Run: cd backend; python -m pytest tests/api/core/test_loans.py -q. Record the missing loan-service failure.
3. Implement the loan migration, domain transitions, service and API contract.
4. Run the focused API test and audit/outbox integration test.

## Evidence checkpoint

- Self-service requests cannot checkout without approval.
- Desk checkout invokes the same checkout service after its explicit approval action.

## Evidence

- RED: `cd backend; python -m pytest tests/api/core/test_loans.py -q` failed during collection with `ModuleNotFoundError: No module named 'openlibrary.modules.core.application.loans'` before circulation loan code existed.
- GREEN: `cd backend; python -m pytest tests/api/core/test_loans.py tests/unit/core/test_loan_service.py tests/unit/migrations/test_loans_migration.py tests/contract/test_loans_openapi.py -q` produced `25 passed in 0.52s`.
  - Self-service loan request creation and authorization (`circulation.request`).
  - Invariant checkpoint: self-service requests cannot checkout without approval (409 Conflict with Problem Details type `https://openlibraryos.example/problems/invalid-loan-status-transition`).
  - Approval action by librarian (`circulation.approve`) transitions status to `approved`.
  - Rejection action by librarian (`circulation.approve`) transitions status to `rejected`.
  - Checkout of approved loan (`circulation.checkout`) transitions loan to `checked_out`, copy status to `borrowed`, and sets `due_at`.
  - Invariant checkpoint: desk checkout (`circulation.checkout`) executes request, explicit approval action, and then the shared checkout service, updating copy status to `borrowed` without bypassing loan invariants or approvals.
  - Return (`circulation.return`) transitions loan to `returned` and copy status atomically back to `available` with append-only copy status history.
  - All successful lifecycle mutations atomically write an `AuditEvent` (`ops.audit_events`) and an `OutboxEvent` (`ops.outbox_events`).
- Full suite: `cd backend; python -m pytest -q` produced `171 passed, 25 skipped in 5.15s`.
- Static analysis & quality gates:
  - `python -m ruff check .` passed cleanly with 0 errors.
  - `python -m ruff format --check .` verified 109 files already formatted.
  - `python -m mypy src` reported `Success: no issues found in 53 source files`.
  - `git diff --check` passed with 0 errors.
- OpenAPI contract:
  - `python -m openapi_spec_validator ../contracts/openapi/v1.yaml` returned `OK`.
  - `python ../contracts/openapi/check_compatibility.py ../contracts/openapi/v1.yaml ../contracts/openapi/v1.yaml` passed with no breaking changes.
- Review: `ocr delegate preview` and `ocr delegate rule` reviewed all reviewable files against system Python ruleset with 0 critical and 0 high findings.

## Acceptance criteria

- Return changes loan and copy state atomically.
- Invalid lifecycle transitions return stable documented errors.
- All successful mutations are authorized and audited.

## Reviewer checklist

- The desk path does not bypass loan invariants.
- Payment and edition-specific conditionals do not enter core loan entities.
