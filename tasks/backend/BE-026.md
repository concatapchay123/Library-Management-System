# BE-026 — Email delivery consumer, bounded retry and worker observability

## Task information

- Owner: Backend
- Status: Completed
- Priority: P2

## Outcome

Deliver notification emails through a replaceable adapter with replay-safe retry and operational visibility.

## Dependencies

- BE-025
- BE-015

## Interfaces and source of truth

- Consumes: notification events from BE-025 and dispatcher/job contract from BE-015.
- Produces: email port, development sink, email delivery consumer, worker readiness and queue metrics.

## In scope

- Define email adapter interface and a development sink.
- Implement idempotent email consumer with bounded exponential retry and dead-letter visibility.
- Expose worker health, attempts, last error and queue/dead-letter metrics.

## Out of scope

- A mandated production email vendor, browser inbox, unbounded retry and changing source transaction outcomes.

## Test-first sequence

1. Add backend/tests/integration/ops/test_email_delivery.py for provider failure, retry, replay and dead-letter behavior.
2. Run: cd backend; python -m pytest tests/integration/ops/test_email_delivery.py -q. Record the missing email-consumer failure.
3. Implement the adapter port, sink, consumer, metrics and worker health endpoint.
4. Run the focused integration test and dispatcher suite.

## Evidence checkpoint

- Red Phase: `backend/tests/integration/ops/test_email_delivery.py` recorded expected missing email consumer failure (`ModuleNotFoundError: No module named 'openlibrary.modules.ops.application.email'`).
- A provider failure does not roll back the originating loan or payment transaction (`test_provider_failure_does_not_rollback_originating_loan_or_payment_transaction` and `test_in_memory_provider_failure_retry_and_dead_letter`).
- Failed jobs expose bounded attempts and actionable last-error metadata (`test_bounded_retry_and_dead_letter_visibility`).
- Replayed domain events create no duplicate email deliveries (`test_email_replay_safety_deduplication` and `test_in_memory_email_replay_deduplication` passing).
- Sensitive data minimization verified: credentials, passwords, raw webhook body, tokens, and card numbers are stripped from email payload and error messages (`test_email_payload_sanitization_removes_secrets` and `test_format_notification_email_scrubs_secrets` passing).
- Worker health and queue metrics exposed (`test_worker_health_and_queue_metrics`, `test_worker_health_http_endpoint` returning 200 OK or 503 RFC Problem Details).
- Worker reconnect and graceful shutdown verified (`test_worker_graceful_shutdown`, `test_worker_reconnect_policy`).
- Test Execution Evidence:
  - `python -m pytest tests/integration/ops/test_email_delivery.py -v`: 8 passed, 6 skipped in 0.55s.
  - `python -m pytest tests/unit/ops/test_email_consumer.py -v`: 11 passed in 0.30s.
  - `python -m pytest tests/unit/ops/test_dispatcher_service.py -v`: 4 passed in 0.29s.
  - `python -m pytest tests/contract/test_openapi_base.py -v`: 1 passed in 0.14s.
  - `python -m pytest tests/contract/ -v`: 10 passed in 0.37s.
  - Full suite `python -m pytest tests/ -q`: 330 passed, 53 skipped in 7.03s.
  - Static analysis: `ruff check .` (All checks passed!), `ruff format --check .` (178 files already formatted), `mypy src` (Success: no issues found in 82 source files), `openapi-spec-validator ../contracts/openapi/v1.yaml` (OK).

## Acceptance criteria

- Development delivery is deterministic without external provider credentials.
- Worker reconnect and graceful shutdown are test-covered.
- Email payloads are replay-safe and contain no credential material.

## Reviewer checklist

- No unbounded retry loop exists.
- A domain handler does not create a parallel queue or outbox protocol.

## Review evidence

- Open Code Review (OCR) delegation preview and rules check performed across all reviewable files with zero high/critical issues.
- All database interactions parameterize values with SQLAlchemy `text(...)` and parameter bindings.
- Multi-tenant boundary preserved with `organization_id` and RLS `set_tenant_context` propagation.
