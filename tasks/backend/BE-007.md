# BE-007 — Argon2id credential validation and login

## Task information

- Owner: Backend
- Status: Completed
- Priority: P0

## Outcome

Authenticate a user inside an organization through Argon2id verification and a uniform public login response.

## Dependencies

- BE-004
- BE-005
- BE-006

## Interfaces and source of truth

- Consumes: POST /api/v1/auth/login contract and core.resolve_login_tenant.
- Produces: user credential persistence, login application service and security audit event.

## In scope

- Migrate tenant-scoped users and profiles with composite tenant constraints and RLS policies.
- Implement Argon2id password hashing and verification.
- Require organization_slug, return a uniform failure for wrong slug, disabled tenant or wrong password, and write security audit events.

## Out of scope

- Access JWT creation, refresh cookies, RBAC and tenant middleware.

## Test-first sequence

1. Add backend/tests/api/auth/test_login.py for success, wrong password, missing slug and duplicate email across organizations.
2. Run: cd backend; python -m pytest tests/api/auth/test_login.py -q. Record the missing login-service failure.
3. Implement user persistence, password service and login use case.
4. Run the focused API test and audit/outbox integration test.

## Evidence checkpoint

- Duplicate emails across separate organizations authenticate only with the correct slug.
- Wrong slug and wrong password return the same public failure.

## Acceptance criteria

- Password plaintext is never stored, returned or logged.
- Missing or disabled slug performs dummy Argon verification.
- Login success and failure security actions are auditable without credential content.

## Reviewer checklist

- [x] No login query bypasses the pre-login resolver boundary.
- [x] User tenant relations use composite foreign keys and RLS in their creation migration.

## Evidence in progress

### TDD RED evidence — 2026-09-21

- Command: `cd backend; python -m pytest tests/api/auth/test_login.py -q`
- Exit code: `1`
- Result: expected `ModuleNotFoundError: No module named 'openlibrary.modules.core.application'` before the login application service existed.

### Local GREEN evidence — 2026-09-21

- Focused command: `cd backend; python -m pytest tests/api/auth/test_login.py tests/unit/core/test_passwords.py tests/unit/core/test_login_service.py tests/unit/migrations/test_users_profiles_migration.py tests/integration/auth/test_login_persistence.py -q`
- Result: `9 passed, 3 skipped`; the skipped cases require the three SQL Server URLs and exercise duplicate tenant emails, audit redaction, composite keys, and RLS catalog assertions.
- Static checks: `python -m ruff format --check .`, `python -m ruff check .`, and `python -m mypy src` exited `0` after the focused suite.

### SQL Server Compose GREEN evidence — 2026-09-21

- Command: `docker compose --project-name openlibrarybe007 -f infra/docker-compose.yml --profile test run --build --rm tests` with disposable SQL Server credentials supplied only through process environment variables.
- Exit code: `0`.
- Result: Alembic applied revisions through `0004_users_profiles`; the container suite reported `12 passed in 9.38s`. This executes the three-identity login persistence suite alongside migration and audit/outbox integration coverage, including duplicate tenant emails, RLS catalog checks, composite user-profile keys, uniform failures, and credential-free audit payloads.
- Cleanup: `docker compose --project-name openlibrarybe007 -f infra/docker-compose.yml --profile test down -v --remove-orphans` exited `0` and removed the disposable database, network, and volume.

### Final local verification and review — 2026-09-21

- Command: `cd backend; python -m pytest -q -rs; python -m ruff format --check .; python -m ruff check .; python -m mypy src`
- Result: `39 passed, 13 skipped`; `50 files already formatted`; Ruff `All checks passed!`; mypy `Success: no issues found in 28 source files`.
- Contract/configuration: `openapi-spec-validator contracts/openapi/v1.yaml`, `python contracts/openapi/check_compatibility.py contracts/openapi/v1.yaml contracts/openapi/v1.yaml`, `docker compose --env-file .github/ci/compose.env -f infra/docker-compose.yml config --quiet`, and `git diff --check` each exited `0`.
- Review: final `ocr delegate preview --format json` listed 16 reviewable and 10 excluded test/document files. The resolved Python/YAML rules and manual review covered all 26 files; no critical or high finding remains.

### CI and Compose integration wiring — 2026-09-21

- RED: `cd backend; python -m pytest tests/integration/app/test_runtime_config.py::test_compose_keeps_migration_credentials_out_of_runtime_services -q` exited `1` because the test profile command omitted `tests/integration/auth/test_login_persistence.py`.
- GREEN: the same command exited `0` after the Compose test profile and the CI SQL Server job were expanded to run the BE-007 integration suite with the migration and audit/outbox suites.
- Final local recheck: `cd backend; python -m pytest -q -rs; python -m ruff format --check .; python -m ruff check .; python -m mypy src` exited `0` with `39 passed, 13 skipped`; the SQL-dependent cases are credited separately by the Compose result above.
