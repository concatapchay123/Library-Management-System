# BE-002 — Backend runtime configuration and local Compose services

## Task information

- Owner: Backend
- Status: Complete
- Priority: P0

## Outcome

Provide validated local configuration and Compose services for the backend, SQL Server, Redis, worker and edge proxy.

## Dependencies

- BE-001

## Interfaces and source of truth

- Consumes: backend application configuration from BE-001 and docs/deployment.md.
- Produces: environment-variable contract, infra/docker-compose.yml health checks and local runtime commands.

## In scope

- Add names-only environment examples and strict required-setting validation.
- Define SQL Server, Redis, backend and worker service health checks with non-public database and Redis ports.
- Add local Compose configuration for backend services without SPA asset hosting.

## Out of scope

- Schema migrations, production secrets, TLS and final SPA routing.

## Test-first sequence

1. Add backend/tests/integration/app/test_runtime_config.py for absent required settings and safe defaults.
2. Run: cd backend; python -m pytest tests/integration/app/test_runtime_config.py -q. Record the validation failure.
3. Implement configuration parsing and Compose service definitions.
4. Run the focused test and docker compose -f infra/docker-compose.yml config.

## Evidence checkpoint

- Focused configuration test passes.
- Compose configuration validation output is stored with the task evidence.

### RED evidence — 2026-09-21

- Command: `cd backend; python -m pytest tests/integration/app/test_runtime_config.py -q`
- Exit code: `2`
- Result: expected collection failure: `ModuleNotFoundError: No module named 'openlibrary.app.runtime'`. The test was added before the runtime configuration implementation.

### GREEN evidence — 2026-09-21

- Command: `cd backend; python -m pytest tests/integration/app/test_runtime_config.py tests/integration/app/test_worker_runtime.py -q`
- Exit code: `0`
- Result: `9 passed in 0.60s`. Required secret and connection names are rejected before app creation, the non-secret environment has an explicit safe default, and the worker uses the validated Redis URL.
- Command: `docker compose -f infra/docker-compose.yml config` with required values supplied only as transient process environment values.
- Exit code: `0`
- Result: Compose resolved `database`, `redis`, `app`, `worker`, and `nginx`; every service has a health check and dependency ordering. Only `nginx` publishes a host port, so SQL Server and Redis remain internal. Sensitive values were redacted from captured output.

### Verification evidence — 2026-09-21

- RED hardening check: `cd backend; python -m pytest tests/integration/app/test_runtime_config.py::test_compose_requires_redis_url -q` exited `1` because Compose accepted a missing `REDIS_URL` through its fallback.
- GREEN focused checks: `cd backend; python -m pytest tests/integration/app/test_runtime_config.py tests/integration/app/test_worker_runtime.py -q` exited `0` with `10 passed in 0.71s` after the fallback was removed.
- Full backend checks: `cd backend; python -m pytest -q`, `python -m ruff check .`, and `python -m mypy src` each exited `0`; pytest reported `13 passed in 0.71s`, Ruff reported `All checks passed!`, and mypy reported `Success: no issues found in 7 source files`.
- Compose validation: `docker compose -f infra/docker-compose.yml config --quiet` with transient test-only values exited `0`. Rendered topology exposed only Nginx port `8080`; every service declared a health check and service-health dependency ordering where applicable.
- `git diff --check` exited `0`. Delegated Open Code Review covered all 11 BE-002 files; no critical, high, or medium finding remained.

## Acceptance criteria

- Missing required runtime settings fail before the server accepts requests.
- Database and Redis are not publicly exposed by default.
- Local services have explicit health checks and dependency order.

## Reviewer checklist

- [x] No secret value is committed.
- [x] Configuration has one documented name for each setting and no silent fallback for secret material.
