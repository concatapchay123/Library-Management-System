# BE-010 — Data-driven RBAC and authorization service

## Task information

- Owner: Backend
- Status: Completed
- Priority: P0

## Outcome

Authorize protected use cases through tenant-scoped role and permission data rather than hard-coded role names.

## Dependencies

- BE-008
- BE-005

## Interfaces and source of truth

- Consumes: protected principal from BE-008 and API permission matrix in docs/api-design.md.
- Produces: roles, permissions, user-role and role-permission persistence plus application authorization port.

## In scope

- Migrate tenant-scoped RBAC relations with composite foreign keys and RLS policies.
- Implement assignment, revocation and effective-permission evaluation.
- Enforce permission decisions at the application-service boundary and audit RBAC mutations.

## Out of scope

- Organization settings, catalog permissions, frontend navigation hiding and platform-control operations.

## Test-first sequence

1. Add backend/tests/api/core/test_rbac.py for assignment, revocation and authorization matrix denial.
2. Run: cd backend; python -m pytest tests/api/core/test_rbac.py -q. Record the missing authorization-port failure.
3. Implement RBAC persistence, service operations and API adapters.
4. Run the focused API test and RBAC migration integration test.

## Evidence checkpoint

- A permission change takes effect without a code deployment.
- Unauthorized requests return a stable authorization failure without data leakage.

## Evidence

- RED: `cd backend; python -m pytest tests/api/core/test_rbac.py -q` failed during collection with `ModuleNotFoundError: No module named 'openlibrary.modules.core.application.authorization'` before implementation. A later HTTP-boundary RED test returned `500` before the explicit `AuthorizationDenied` handler was added.
- GREEN: `cd backend; python -m pytest tests/api/core/test_rbac.py tests/unit/migrations/test_rbac_migration.py tests/integration/migrations/test_rbac_schema.py -q` produced `2 passed, 1 skipped` without SQL URLs; the new application tests cover immediate revocation, named-permission denial, audit action intent, and non-leaking `403` Problem Details.
- SQL Server: with a locally-created disposable bootstrap login, temporary migration/runtime identities, and disposable database, `cd backend; python -m pytest tests/integration/migrations/test_rbac_schema.py -q` produced `2 passed in 6.65s`. It verified all four RBAC tables' RLS catalog entries and composite foreign keys, live permission evaluation, immediate revocation, and tenant-scoped assignment/revocation audit rows. All temporary identities and the database were deleted after the run.
- Full local quality: under the same disposable SQL Server environment, `cd backend; python -m pytest -q` produced `89 passed in 37.43s` with no skips. `python -m ruff format --check .`, `python -m ruff check .`, `python -m mypy src`, `python -m openapi_spec_validator ../contracts/openapi/v1.yaml`, `docker compose --env-file .github/ci/compose.env -f infra/docker-compose.yml config --quiet`, and `git diff --check` passed.
- Review: delegated Open Code Review preview covered 8/8 reviewable implementation/configuration files; the remaining 6 test/task files were excluded by the tool's default-path/extension rules and were reviewed through focused and full pytest evidence. No critical or high findings remain.

## Acceptance criteria

- Services request named permissions through one authorization port.
- No protected behavior relies on a hard-coded role name.
- RBAC mutations produce tenant-scoped audit records.

## Reviewer checklist

- [x] Route permission checks do not replace service ownership checks.
- [x] All RBAC tenant tables have RLS and composite tenant constraints at creation.
