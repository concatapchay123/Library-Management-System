# BE-006 — OpenAPI base and backend CI quality gates

## Task information

- Owner: Backend
- Status: Completed
- Priority: P0

## Outcome

Publish the versioned API base contract and automate the backend checks needed to keep it compatible.

## Dependencies

- BE-005

## Interfaces and source of truth

- Consumes: docs/api-design.md, backend commands from BE-001 and migration controls from BE-003.
- Produces: contracts/openapi/v1.yaml, backend CI workflow and documented compatibility command.

## In scope

- Define health and authentication contract foundations, RFC Problem Details and request-correlation headers.
- Add CI jobs for backend formatting, linting, typing, unit/API tests, migration integration, OpenAPI validation and Compose validation.
- Make contract compatibility checks block unversioned breaking API changes.

## Out of scope

- Business-resource endpoint implementation, frontend client generation and release deployment.

## Test-first sequence

1. Add backend/tests/contract/test_openapi_base.py for required health, Problem Details and request-id contract elements.
2. Run: cd backend; python -m pytest tests/contract/test_openapi_base.py -q. Record the missing contract failure.
3. Implement the contract base and CI workflow.
4. Run the focused contract test and the documented OpenAPI validation command.

## Evidence checkpoint

- Contract validation output is recorded.
- CI contains a blocking job for every backend command defined by BE-001 and BE-003.

### RED evidence — 2026-09-21

- Command: `cd backend; python -m pytest tests/contract/test_openapi_base.py -q`
- Exit code: `1`
- Result: expected `FileNotFoundError` for the missing
  `contracts/openapi/v1.yaml` base contract.

### GREEN and final verification — 2026-09-21

- `cd backend; python -m pytest -q` → `22 passed, 6 skipped in 1.40s`.
- `cd backend; python -m ruff format --check .` → `29 files already formatted`.
- `cd backend; python -m ruff check .` → `All checks passed!`.
- `cd backend; python -m mypy src` → `Success: no issues found in 16 source files`.
- `openapi-spec-validator contracts/openapi/v1.yaml` →
  `contracts/openapi/v1.yaml: OK`.
- `python contracts/openapi/check_compatibility.py contracts/openapi/v1.yaml contracts/openapi/v1.yaml`
  → `OpenAPI compatibility check passed.`
- `docker compose --env-file .github/ci/compose.env -f infra/docker-compose.yml config --quiet`
  exited `0`.
- A temporary local SQL Server ran
  `cd backend; python -m pytest tests/integration/migrations/test_database_roles.py -q`
  → `3 passed in 5.63s`.
- `.github/workflows/backend-quality.yml` contains blocking backend quality,
  SQL Server migration integration, OpenAPI validation/compatibility, and
  Compose validation jobs. Its YAML parsed with all four required job IDs.

## Acceptance criteria

- API base URL is /api/v1.
- Error responses follow RFC Problem Details with a request_id.
- CI fails when backend, migration, contract or Compose checks fail.

## Reviewer checklist

- [x] Contract changes are machine-readable and versioned.
- [x] CI never substitutes frontend checks for backend release gates.
