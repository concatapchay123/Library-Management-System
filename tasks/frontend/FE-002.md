# FE-002 — App shell, familiar navigation and accessible shared controls

## Task information

- Owner: Frontend
- Status: Completed
- Priority: P2

## Outcome

Provide a simple application shell with familiar navigation and reusable accessible controls for low-tech users.

## Dependencies

- FE-001

## Interfaces and source of truth

- Consumes: PRODUCT.md, DESIGN.md and frontend tokens from FE-001.
- Produces: app layout, navigation model, button/input/dialog primitives and responsive focus behavior.

## In scope

- Add logo-to-home navigation, primary navigation, page heading region and account area.
- Build accessible button, labeled input, status message and dialog controls with Escape dismissal.
- Limit each screen region to one visually primary action and group related fields with token spacing.

## Out of scope

- Authentication, API state, catalog content, visual effects and custom design-system expansion.

## Test-first sequence

1. Add frontend/tests/app/shell.test.tsx for landmark order, logo home link, keyboard navigation and Escape dialog close.
2. Run: cd frontend; npm run test -- --run tests/app/shell.test.tsx. Record the missing shell failure.
3. Implement shell, navigation and shared controls.
4. Run the focused test, npm run typecheck, npm run lint and npm run build.

## Evidence checkpoint

- **Step 2 (Red Phase) Missing Shell Failure**:
  ```text
  > cd frontend; npm run test -- --run tests/app/shell.test.tsx
  FAIL tests/app/shell.test.tsx [ tests/app/shell.test.tsx ]
  Error: Failed to resolve import "../../src/app/navigation/navigationModel" from "tests/app/shell.test.tsx". Does the file exist?
  Plugin: vite:import-analysis
  File: E:/Project/Online Library Management System/frontend/tests/app/shell.test.tsx:6:32
  Test Files  1 failed (1)
       Tests  no tests
  ```

- **Step 4 (Green Phase) Focused Shell Test**:
  ```text
  > cd frontend; npm run test -- --run tests/app/shell.test.tsx
  RUN  v3.2.7 E:/Project/Online Library Management System/frontend
  ✓ tests/app/shell.test.tsx (18 tests) 202ms

  Test Files  1 passed (1)
       Tests  18 passed (18)
  ```

- **Keyboard Traversal & Focus Evidence**:
  - Traversal order verified: Skip Link (`#main-content`) -> Banner (`<header role="banner">`) -> Primary Navigation (`<nav aria-label="Primary navigation">`) -> Main Content (`<main id="main-content">`) -> Primary Action.
  - Dialog modal accessibility verified: `role="dialog"`, `aria-modal="true"`, `aria-labelledby`, `aria-describedby`.
  - Dialog Escape key dismissal verified with focus restoration to trigger element (`document.activeElement === trigger`).
  - Dialog Close button dismissal verified with focus restoration to trigger element.

- **Full Frontend Verification Suite**:
  - `npm run test`:
    ```text
    ✓ tests/app/bootstrap.test.tsx (9 tests) 84ms
    ✓ tests/app/shell.test.tsx (18 tests) 202ms
    Test Files  2 passed (2)
         Tests  27 passed (27)
    ```
  - `npm run typecheck`: `tsc --noEmit` passed with 0 errors.
  - `npm run lint`: `eslint .` passed with 0 errors, 0 warnings.
  - `npm run build`: `tsc --noEmit && vite build` completed successfully, producing production bundle (`dist/`).

- **Delegated Open Code Review (OCR)**:
  - Total files evaluated: 13 (11 reviewable, 2 excluded).
  - Reviewable files checked against Rule Group 1 (`**/*.{ts,js,tsx,jsx,mjs,cjs}`):
    - `frontend/src/app/App.tsx`: Reviewed (no dead code, no duplicate headers, proper landmark order, 1 primary action).
    - `frontend/src/shared/tokens/types.ts`: Reviewed (StatusVariant type export, strict types).
    - `frontend/src/app/navigation/navigationModel.ts`: Reviewed (product language, explicit authorization decoupling invariant).
    - `frontend/src/app/shell/AppHeader.tsx`: Reviewed (banner landmark, accessible navigation, account area).
    - `frontend/src/app/shell/AppShell.tsx`: Reviewed (skip link, banner, main landmark, heading region).
    - `frontend/src/app/shell/index.ts`: Reviewed (clean exports).
    - `frontend/src/shared/components/Button.tsx`: Reviewed (2:1 padding ratio, focus ring, title case, danger variant).
    - `frontend/src/shared/components/Dialog.tsx`: Reviewed (modal semantics, Escape key listener cleanup, focus return).
    - `frontend/src/shared/components/Input.tsx`: Reviewed (1-column layout, label-input programmatic binding, optional tag).
    - `frontend/src/shared/components/StatusMessage.tsx`: Reviewed (non-color cues, role="status" and "alert").
    - `frontend/src/shared/components/index.ts`: Reviewed (clean exports).
  - Review status: 11 reviewed, 0 critical findings, 0 high findings, 0 medium findings, 0 low findings.
  - Coverage rate: 100% of reviewable files.

- **System Regression Checks**:
  - `python -m pytest backend/tests/integration/release/test_release_gate.py -k test_frontend_accessibility_and_release_contract_verification`: PASSED (1 passed, 8 deselected in 0.33s).
  - `python -m pytest backend/tests/ -q`: PASSED (345 passed, 53 skipped in 6.87s).

## Acceptance criteria

- [x] Navigation labels use product language rather than technical identifiers.
- [x] Shared controls expose labels, descriptions and validation messages programmatically.
- [x] Layout remains usable at narrow viewport widths without hiding primary actions.

## Reviewer checklist

- [x] One screen region does not contain competing primary buttons.
- [x] No authorization decision is inferred from navigation visibility.
