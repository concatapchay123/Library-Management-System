# FE-008 — Reservation status and notification inbox

## Task information

- Owner: Frontend
- Status: Completed
- Priority: P2

## Outcome

Show users their reservation progression and time-sensitive notifications in one concise inbox.

## Dependencies

- FE-007

## Interfaces and source of truth

- Consumes: reservation endpoints from BE-018 and notification endpoints from BE-025.
- Produces: reservation status route, notification inbox and read-state interaction.

## In scope

- Render reservation queue/hold status using backend-provided state and dates.
- Present notification list, unread/read distinction and mark-read operation.
- Provide empty, loading and failure states with one clear next action.

## Out of scope

- Email delivery configuration, queue reordering, push notifications and client-side hold-expiry calculation.

## Test-first sequence

1. Add frontend/tests/features/inbox/reservation-inbox.test.tsx for hold display, notification read transition and empty inbox.
2. Run: cd frontend; npm run test -- --run tests/features/inbox/reservation-inbox.test.tsx. Record the missing inbox failure.
3. Implement reservation and notification routes using typed data.
4. Run the focused test, npm run typecheck, npm run lint and npm run build.

## Evidence checkpoint

- Marking a notification read updates only after a successful server response.
- No-reservation and no-notification states use distinct, understandable copy.

## Evidence

- RED: `cd frontend; npm run test -- --run tests/features/inbox/reservation-inbox.test.tsx` failed as expected before feature implementation:
  ```
  FAIL  tests/features/inbox/reservation-inbox.test.tsx [ tests/features/inbox/reservation-inbox.test.tsx ]
  Error: Failed to resolve import "../../../src/features/inbox" from "tests/features/inbox/reservation-inbox.test.tsx". Does the file exist?
  ```
- GREEN: After implementing typed client reservations & notifications modules, `ReservationStatusCard`, `NotificationCard`, `ReservationInbox`, and integrating with `App.tsx`:
  ```
  RUN  v3.2.7 E:/Project/Online Library Management System/frontend
  ✓ tests/features/inbox/reservation-inbox.test.tsx (12 tests) 385ms
  Test Files  1 passed (1)
  Tests  12 passed (12)
  ```
- Full suite verification:
  - Vitest: 103 passed across 8 test files (`api-client.test.ts`, `bootstrap.test.tsx`, `shell.test.tsx`, `auth-flow.test.tsx`, `reservation-inbox.test.tsx`, `catalog-search.test.tsx`, `circulation-desk.test.tsx`, `inventory-workspace.test.tsx`).
  - TypeScript: `npm run typecheck` (`tsc --noEmit`) exited 0 with no diagnostic errors.
  - Lint: `npm run lint` (`eslint .`) exited 0 with 0 warnings and 0 errors.
  - Production build: `npm run build` (`tsc --noEmit && vite build`) produced optimized bundle (`dist/assets/index-BYQEqxcb.js`, 256.83 kB │ gzip: 70.57 kB) cleanly in 757ms.
  - Backend contract verification: `python -m pytest tests/contract/test_reservations_openapi.py tests/contract/test_notifications_openapi.py -q` passed with 2 passed; `ruff`, `mypy`, and `openapi_spec_validator` passed with 0 errors.
- Evidence checkpoints verified:
  - Marking a notification read updates only after a successful server response: confirmed via test asserting status transition to 'read' only upon 200 OK from server, and remaining in 'unread' status with RFC Problem Details rendered when server responds with 500 error.
  - No-reservation and no-notification states use distinct, understandable copy: confirmed via assertions validating distinct messages ("No active or past reservations found..." with "Explore Catalog" CTA vs "Your notification inbox is clear...").
  - Reservation status does not promise a pickup or allocation beyond server facts: pending reservations display server queue position without promised dates; held reservations display server `hold_expires_at` verbatim.
  - Inbox contains one dominant action: verified that both reservation and notification cards provide one primary CTA ("Open Relevant Record") to view the underlying entity without treating notifications as authorization truth.
  - Read-state controls are keyboard accessible: verified via native button handling, focus ring tokens, and Enter key event triggering.

## Acceptance criteria

- Reservation status does not promise a pickup or allocation beyond server facts.
- Inbox contains one dominant action: open the relevant record.
- Read-state controls are keyboard accessible.

## Reviewer checklist

- [x] UI does not expose another user's notification or reservation data.
- [x] Notifications are not treated as a source of authorization truth.

## Review evidence

- Open Code Review (OCR) delegation preview and rules check performed across all 8 reviewable files with 100% coverage and zero high/critical issues.
- Client requests explicitly transmit Bearer token and enforce tenant scoping; records opened via notification routes delegate authorization and domain validation authoritatively to backend endpoints.
