# FE-001 — Frontend bootstrap, product UI context, tokens and test tooling

## Task information

- Owner: Frontend
- Status: Completed
- Priority: P2

## Outcome

Create the React/TypeScript workspace, durable product UI context, foundational tokens and deterministic frontend checks.

## Dependencies

- BE-026

## Interfaces and source of truth

- Consumes: DESIGN.md, docs/architecture.md and the complete OpenAPI contract.
- Produces: frontend package, PRODUCT.md, application token layer and frontend test/type/lint/build commands.

## In scope

- Initialize React, TypeScript, test runner, type-check, lint and production-build scripts.
- Record an Operate-mode product UI brief in PRODUCT.md derived only from DESIGN.md.
- Define spacing, typography, color and focus tokens for a low-tech, accessible operating interface.

## Out of scope

- App navigation, login screen, API calls, feature screens and visual rebranding.

## Test-first sequence

1. Add frontend/tests/app/bootstrap.test.tsx that imports the root application and token provider.
2. Run: cd frontend; npm run test -- --run tests/app/bootstrap.test.tsx. Record the missing workspace failure.
3. Implement the smallest frontend bootstrap, product context and token layer.
4. Run the focused test, npm run typecheck, npm run lint and npm run build.

## Evidence checkpoint

- **Step 2 (Red Phase) Missing Workspace Failure**:
  ```text
  > cd frontend; npm run test -- --run tests/app/bootstrap.test.tsx
  npm error code ENOENT
  npm error syscall open
  npm error path E:\Project\Online Library Management System\frontend\package.json
  npm error errno -4058
  npm error enoent Could not read package.json: Error: ENOENT: no such file or directory, open 'E:\Project\Online Library Management System\frontend\package.json'
  ```

- **Step 4 (Green Phase) Focused Bootstrap Test**:
  ```text
  > cd frontend; npm run test -- --run tests/app/bootstrap.test.tsx
  RUN  v3.2.7 E:/Project/Online Library Management System/frontend
  ✓ tests/app/bootstrap.test.tsx (9 tests) 58ms

  Test Files  1 passed (1)
       Tests  9 passed (9)
  ```

- **Typecheck, Lint & Production Build**:
  - `npm run typecheck`: `tsc --noEmit` passed with 0 errors.
  - `npm run lint`: `eslint .` passed with 0 errors, 0 warnings.
  - `npm run build`: `tsc --noEmit && vite build` completed successfully, producing production bundle (`dist/`).

- **Durable Product Context & Invariants**:
  - `PRODUCT.md` created, defining audience (Platform Admin, Org Admin, Librarian, Student/Teacher, Member, Contributor/Operator), editions (Core, Education, Public Library), Operate-mode low-cognitive-overhead UI context, minimum-comprehension UI rule, and strict V1 boundaries derived strictly from `DESIGN.md`.

- **Delegated Open Code Review (OCR)**:
  - Total files evaluated: 18 (13 reviewable, 5 excluded).
  - Review status: 13 reviewed, 0 critical findings, 0 high findings, 0 warnings.

- **System Regression Checks**:
  - `python -m pytest backend/tests/ -q`: 345 passed, 53 skipped.
  - `python -m pytest backend/tests/integration/release/test_release_gate.py -k test_frontend_accessibility_and_release_contract_verification`: PASSED.
  - `python -m ruff check backend`: All checks passed.
  - `python -m ruff format --check backend`: 180 files already formatted.
  - `python -m mypy backend/src`: Success: no issues found in 83 source files.
  - `powershell -ExecutionPolicy Bypass -File infra/tests/test_production_release.ps1`: Summary: 6 passed, 0 failed out of 6 checks.
  - `python contracts/openapi/check_compatibility.py contracts/openapi/v1.yaml contracts/openapi/v1.yaml`: Passed.

## Acceptance criteria

- [x] Tokens use a consistent spacing scale and visible keyboard focus.
- [x] The application can render without a network request.
- [x] PRODUCT.md does not invent product scope beyond DESIGN.md.

## Reviewer checklist

- [x] No business rules or API endpoint strings are embedded in token code.
- [x] Tokens support readable contrast and non-color status cues.
