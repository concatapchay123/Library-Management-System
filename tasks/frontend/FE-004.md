# FE-004 — Typed API client, Problem Details and common request states

## Task information

- Owner: Frontend
- Status: Completed
- Priority: P2

## Outcome

Centralize typed contract access and make loading, empty, error and request-correlation states understandable before feature screens are added.

## Dependencies

- FE-003

## Interfaces and source of truth

- Consumes: contracts/openapi/v1.yaml and authenticated session state from FE-003.
- Produces: typed API client, Problem Details renderer, request-state components and request_id support display.

## In scope

- Generate or implement one typed API client from OpenAPI.
- Normalize RFC Problem Details into concise user messages with a visible support request_id.
- Add reusable loading, empty, retry and non-destructive error states.

## Out of scope

- Duplicated endpoint string literals in feature code, domain-policy evaluation and feature-specific screens.

## Test-first sequence

1. Add frontend/tests/shared/api-client.test.ts for typed request serialization and Problem Details mapping.
2. Run: cd frontend; npm run test -- --run tests/shared/api-client.test.ts. Record the missing API-client failure.
3. Implement client, error mapping and common request-state components.
4. Run the focused test, npm run typecheck, npm run lint and npm run build.

## Evidence checkpoint

- **Step 2 (Red Phase) Missing API-Client Failure**:
  ```text
  > cd frontend; npm run test -- --run tests/shared/api-client.test.ts
  FAIL  tests/shared/api-client.test.ts [ tests/shared/api-client.test.ts ]
  Error: Failed to resolve import "../../src/shared/api" from "tests/shared/api-client.test.ts". Does the file exist?
    Plugin: vite:import-analysis
    File: E:/Project/Online Library Management System/frontend/tests/shared/api-client.test.ts:11:7
  Test Files  1 failed (1)
       Tests  no tests
  ```

- **Step 4 (Green Phase) Focused API-Client Test**:
  ```text
  > cd frontend; npm run test -- --run tests/shared/api-client.test.ts
  RUN  v3.2.7 E:/Project/Online Library Management System/frontend
  ✓ tests/shared/api-client.test.ts (15 tests) 95ms

  Test Files  1 passed (1)
       Tests  15 passed (15)
  ```

- **Documented Problem Details Sample**:
  - Sample Problem Details payload from OpenAPI v1 contract:
    ```json
    {
      "type": "https://openlibraryos.example/problems/not-found",
      "title": "Book Copy Not Found",
      "status": 404,
      "detail": "The requested copy identifier was not found in catalog circulation.",
      "instance": "/api/v1/circulation/copies/12345",
      "request_id": "SUPPORT-REQ-12345-ABC"
    }
    ```
  - Rendered output via `ProblemDetailsRenderer`:
    - **Status & Type**: Non-color status cue (`✕` badge + `ERROR` + `Status 404`).
    - **Title**: `"Book Copy Not Found"`.
    - **Detail**: `"The requested copy identifier was not found in catalog circulation."`.
    - **Helpful Action**: `"Check the identifier or link."` (derived via `deriveHelpfulAction(404)`).
    - **Support Request ID**: `<div data-testid="support-request-id">Support Request ID: <code>SUPPORT-REQ-12345-ABC</code> <button>Copy ID</button></div>`.
    - **Retry Action**: Enabled (`isSafeToRetry: true` for safe `GET /api/v1/...`).

- **Feature Modules Import Client Rather Than Assembling Endpoint Strings**:
  - `frontend/src/features/auth/authApi.ts` refactored to import `apiClient` from `../../shared/api`:
    - `apiClient.auth.login({ organization_slug, email, password })` replaces `fetch('/api/v1/auth/login')`.
    - `apiClient.auth.refreshAccessToken()` replaces `fetch('/api/v1/auth/refresh')`.
    - `apiClient.auth.logout(accessToken)` replaces `fetch('/api/v1/auth/logout')`.
  - Zero raw endpoint string literals remain in auth feature code.

- **Security & Architectural Invariants Verified**:
  - **Sanitization of SQL Queries & Database Traces**: Verified that queries like `SELECT * FROM users WHERE organization_id = 42 failed with ODBC Driver 17` are redacted to `"A database error occurred. Please contact support with the request ID."` without exposing schema or database internals.
  - **Sanitization of Stack Traces & Runtime Exceptions**: Verified that Python `Traceback` lines and Node runtime frames are sanitized to `"An unexpected server error occurred. Please contact support with the request ID."`.
  - **Protection of Credentials & Token Data**: Verified that JWT signatures, `Bearer` tokens, and secrets are stripped and replaced with concise user-facing error messages.
  - **Decoupled Authorization Decision**: Verified that error normalization preserves backend authorization (e.g., status 403 Forbidden remains strictly 403) and does not override or mutate backend access control or financial decisions.
  - **Safe Retry Verification**: Verified that retry controls (`isSafeToRetry`) are restricted strictly to safe idempotent requests (`GET`, `HEAD`, `OPTIONS`). Mutating state changes (`POST`, `PUT`, `DELETE`) without explicit idempotency guarantees suppress retry controls to prevent duplicate operations (such as accidental double-borrowing or duplicate charges).
  - **Request Correlation**: Client automatically injects an `X-Request-ID` header matching OpenAPI pattern `^[A-Za-z0-9._-]{1,128}$` and extracts server-returned correlation identifiers.

- **Cognitive & Visual Invariants Verified**:
  - **Jakob's Law**: Familiar conventions for support correlation, copy button, and error alerts.
  - **Hick's Law**: 01 Primary Action on empty states and error recovery, Title Case labels.
  - **Law of Proximity**: Semantic spacing tokens (12px / 24px / 32px) grouping status messages, details, and action triggers.
  - **Button 2:1 Whitespace Ratio**: $px = 2 \times py$ enforced via design tokens.
  - **Color & Contrast**: WCAG AA ($>4.5:1$), zero pure black (`#000000`) and pure white (`#ffffff`).
  - **Loading Skeleton**: Shimmer skeleton with `role="status"` and `aria-busy="true"` replacing central spinners.

- **Full Frontend Verification Suite**:
  - `npm run test`:
    ```text
    ✓ tests/app/bootstrap.test.tsx (9 tests) 84ms
    ✓ tests/shared/api-client.test.ts (15 tests) 97ms
    ✓ tests/app/shell.test.tsx (18 tests) 190ms
    ✓ tests/features/auth/auth-flow.test.tsx (10 tests) 223ms

    Test Files  4 passed (4)
         Tests  52 passed (52)
    ```
  - `npm run typecheck`: `tsc --noEmit` passed with 0 errors.
  - `npm run lint`: `eslint .` passed with 0 errors, 0 warnings.
  - `npm run build`: `tsc --noEmit && vite build` completed successfully, producing production bundle (`dist/index.html` 0.39 kB, `dist/assets/index-CUd-i7fW.js` 176.40 kB).

- **Delegated Open Code Review (OCR)**:
  - Total files evaluated: 10 (8 reviewable source files, 1 test file, 1 configuration).
  - Reviewable files checked against Rule Group 1 (`**/*.{ts,js,tsx,jsx,mjs,cjs}`):
    - `frontend/src/shared/api/types.ts`: Reviewed (clean OpenAPI contract derivation, no loose anys).
    - `frontend/src/shared/api/problemDetails.ts`: Reviewed (sanitizes SQL/traces/tokens, regex boundary safety, prototype chain preserved).
    - `frontend/src/shared/api/apiClient.ts`: Reviewed (correlation header handling, 204 No Content safety, credential isolation).
    - `frontend/src/shared/api/index.ts`: Reviewed (clean exports barrel).
    - `frontend/src/shared/components/ProblemDetailsRenderer.tsx`: Reviewed (visible support request_id, safe copy handling, non-color status cues, safe retry gating).
    - `frontend/src/shared/components/LoadingSkeleton.tsx`: Reviewed (accessible shimmer loading, aria-busy compliance, zero pure black/white).
    - `frontend/src/shared/components/EmptyState.tsx`: Reviewed (states what is absent, single primary action, 2:1 button ratio).
    - `frontend/src/shared/components/RequestStateView.tsx`: Reviewed (clean state branching, retry safety enforcement, zero duplicate submits).
    - `frontend/src/shared/components/index.ts`: Reviewed (clean component exports).
    - `frontend/src/features/auth/authApi.ts`: Reviewed (imports client, zero literal URLs, safe uniform error preservation).
    - `frontend/tests/shared/api-client.test.ts`: Reviewed (15 comprehensive integration tests, strict mock assertions).
  - Review status: 0 critical findings, 0 high findings, 0 medium findings, 0 low findings.
  - Coverage rate: 100% of reviewable files.

- **System Regression Checks**:
  - `python -m pytest backend/tests/ -q`: PASSED (345 passed, 53 skipped in 6.71s).

## Acceptance criteria

- [x] User-facing errors are concise and do not expose SQL, stack traces or token data.
- [x] Empty states state what is absent and the one next action when one is permitted.
- [x] Retry controls repeat only safe documented requests.

## Reviewer checklist

- [x] API types remain derived from the versioned contract.
- [x] Client error normalization never changes backend authorization or money decisions.
