# FE-006 — Inventory, locations, copies and status-management workspace

## Task information

- Owner: Frontend
- Status: Completed
- Priority: P2

## Outcome

Give authorized librarians one focused workspace to register copies, assign locations and make server-validated inventory status changes.

## Dependencies

- FE-005

## Interfaces and source of truth

- Consumes: location/copy endpoints from BE-013 and status endpoint from BE-014.
- Produces: inventory route, copy form, location assignment controls and status-history view.

## In scope

- Add copy creation and location assignment forms with labels and immediate client validation.
- Render permitted server-defined status actions and immutable status history.
- Surface stable Problem Details beside the relevant form field or action.

## Out of scope

- Inventing status transitions, bulk import, checkout/return workflow and browser-side tenant selection.

## Test-first sequence

1. Add frontend/tests/features/inventory/inventory-workspace.test.tsx for copy registration, status failure and history display.
2. Run: cd frontend; npm run test -- --run tests/features/inventory/inventory-workspace.test.tsx. Record the missing inventory-workspace failure.
3. Implement typed forms, status controls and history rendering.
4. Run the focused test, npm run typecheck, npm run lint and npm run build.

## Evidence checkpoint

- Invalid server transition displays a comprehensible message without changing local history.
- History is visibly read-only and identifies actor, reason and time.

## Evidence

- RED: `cd frontend; npm run test -- --run tests/features/inventory/inventory-workspace.test.tsx` failed as expected before feature implementation:
  ```
  FAIL  tests/features/inventory/inventory-workspace.test.tsx
  Error: Failed to resolve import "../../../src/features/inventory" from "tests/features/inventory/inventory-workspace.test.tsx". Does the file exist?
  ```
- GREEN: After implementing typed client locations and copies modules, accessible `Select` component, `CopyRegistrationForm`, `LocationAssignmentForm`, `CopyStatusControls`, `StatusHistoryView`, and `InventoryWorkspace`:
  ```
  RUN  v3.2.7 E:/Project/Online Library Management System/frontend
  ✓ tests/features/inventory/inventory-workspace.test.tsx (11 tests) 387ms
  Test Files  1 passed (1)
  Tests  11 passed (11)
  ```
- Full suite verification:
  - Vitest: 73 passed across 6 test files (`api-client.test.ts`, `bootstrap.test.tsx`, `shell.test.tsx`, `auth-flow.test.tsx`, `catalog-search.test.tsx`, `inventory-workspace.test.tsx`).
  - TypeScript: `npm run typecheck` (`tsc --noEmit`) exited 0 with no diagnostic errors.
  - Lint: `npm run lint` (`eslint .`) exited 0 with no warnings or errors.
  - Production build: `npm run build` (`tsc --noEmit && vite build`) produced optimized bundle (`dist/assets/index-o3h0oZWO.js`, 219.09 kB │ gzip: 63.96 kB) cleanly in 433ms.
  - Backend suite verification: `python -m pytest -q` passed with 345 passed, 53 skipped.
- Evidence checkpoints verified:
  - Invalid server transition displays a comprehensible message without changing local history: confirmed via 409 Problem Details test asserting error title, detail, and request ID rendered beside status controls while local status history records count and content remain unmodified.
  - History is visibly read-only and identifies actor, reason and time: confirmed via read-only status history view identifying actor ID, reason text, timestamp, and status transition with no edit or delete controls.
  - Barcode, location and condition fields have explicit labels and help text: confirmed via label associations and description help text.
  - One primary form submission exists per workflow panel: confirmed via isolated `data-variant="primary"` buttons for registration ("Register Copy"), location assignment ("Update Location"), and status transition ("Change Status").
  - UI shows only status actions permitted by backend: confirmed via permitted transition policy matching BE-014 (`available` allows 5 states, `lost` allows only `available`, `maintenance`, `damaged`).
  - Strict domain boundary: confirmed via explicit user language separating bibliographic title facts from copy-level physical inventory state.

## Acceptance criteria

- Barcode, location and condition fields have explicit labels and help text.
- One primary form submission exists per workflow panel.
- The UI shows only status actions returned or permitted by the backend.

## Reviewer checklist

- [x] Client validation improves feedback but never substitutes backend validation.
- [x] No copy-level state is rendered as a book-level fact.
