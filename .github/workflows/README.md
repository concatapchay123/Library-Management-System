# CI workflows

TASK-002 sẽ tạo workflow kiểm tra backend, frontend, OpenAPI, Docker Compose và SQL Server integration. Workflow phải fail closed khi test, type-check, build hoặc contract compatibility fail.
