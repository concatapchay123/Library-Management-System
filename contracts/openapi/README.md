# API contracts

OpenAPI versioned contracts là source of truth cho backend/frontend compatibility. `v1.yaml` sẽ được tạo trong TASK-002 cùng với health/auth foundation; mọi endpoint change phải cập nhật contract và compatibility check.
